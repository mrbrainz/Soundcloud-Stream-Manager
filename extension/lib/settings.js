// Shared settings schema + storage helpers.
// Scaffolded here (empty implementation) so manifest.json has a stable file
// to load; the actual schema and live-apply pub/sub land in the
// "Settings schema + live-apply storage" board card.
